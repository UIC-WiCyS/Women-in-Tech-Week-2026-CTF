import styles from "./ChallengeModal.module.css";

export default function ChallengeModal({session, name, description, category, files, hints, points}) {

    return (
        <div>
            
        </div>
    );
}